enum Auth  {
  Base = 'auth',
  Login = 'login',
  Register = 'register',
  ResetPassword = 'reset-password',
  ForgotPassword = 'forgot-password'
}

enum Account {
  Base = '',
}

export { Auth, Account };
